// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RecordSessionTurnArgs {
    pub session_id: String,
    pub last_prompt: String,
    pub timestamp_ms: i64,
}

impl From<RecordSessionTurnArgs> for super::Reducer {
    fn from(args: RecordSessionTurnArgs) -> Self {
        Self::RecordSessionTurn {
            session_id: args.session_id,
            last_prompt: args.last_prompt,
            timestamp_ms: args.timestamp_ms,
        }
    }
}

impl __sdk::InModule for RecordSessionTurnArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `record_session_turn`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait record_session_turn {
    /// Request that the remote module invoke the reducer `record_session_turn` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`record_session_turn:record_session_turn_then`] to run a callback after the reducer completes.
    fn record_session_turn(
        &self,
        session_id: String,
        last_prompt: String,
        timestamp_ms: i64,
    ) -> __sdk::Result<()> {
        self.record_session_turn_then(session_id, last_prompt, timestamp_ms, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `record_session_turn` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn record_session_turn_then(
        &self,
        session_id: String,
        last_prompt: String,
        timestamp_ms: i64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl record_session_turn for super::RemoteReducers {
    fn record_session_turn_then(
        &self,
        session_id: String,
        last_prompt: String,
        timestamp_ms: i64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RecordSessionTurnArgs {
                session_id,
                last_prompt,
                timestamp_ms,
            },
            callback,
        )
    }
}
