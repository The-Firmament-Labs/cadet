// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RecordTrainingRunArgs {
    pub training_id: String,
    pub base_model: String,
    pub adapter_path: String,
    pub method: String,
    pub trajectory_count: u32,
    pub group_count: u32,
    pub epochs: u32,
    pub final_loss: f32,
    pub delight_threshold: f32,
    pub duration_seconds: u32,
    pub status: String,
}

impl From<RecordTrainingRunArgs> for super::Reducer {
    fn from(args: RecordTrainingRunArgs) -> Self {
        Self::RecordTrainingRun {
            training_id: args.training_id,
            base_model: args.base_model,
            adapter_path: args.adapter_path,
            method: args.method,
            trajectory_count: args.trajectory_count,
            group_count: args.group_count,
            epochs: args.epochs,
            final_loss: args.final_loss,
            delight_threshold: args.delight_threshold,
            duration_seconds: args.duration_seconds,
            status: args.status,
        }
    }
}

impl __sdk::InModule for RecordTrainingRunArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `record_training_run`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait record_training_run {
    /// Request that the remote module invoke the reducer `record_training_run` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`record_training_run:record_training_run_then`] to run a callback after the reducer completes.
    fn record_training_run(
        &self,
        training_id: String,
        base_model: String,
        adapter_path: String,
        method: String,
        trajectory_count: u32,
        group_count: u32,
        epochs: u32,
        final_loss: f32,
        delight_threshold: f32,
        duration_seconds: u32,
        status: String,
    ) -> __sdk::Result<()> {
        self.record_training_run_then(
            training_id,
            base_model,
            adapter_path,
            method,
            trajectory_count,
            group_count,
            epochs,
            final_loss,
            delight_threshold,
            duration_seconds,
            status,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `record_training_run` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn record_training_run_then(
        &self,
        training_id: String,
        base_model: String,
        adapter_path: String,
        method: String,
        trajectory_count: u32,
        group_count: u32,
        epochs: u32,
        final_loss: f32,
        delight_threshold: f32,
        duration_seconds: u32,
        status: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl record_training_run for super::RemoteReducers {
    fn record_training_run_then(
        &self,
        training_id: String,
        base_model: String,
        adapter_path: String,
        method: String,
        trajectory_count: u32,
        group_count: u32,
        epochs: u32,
        final_loss: f32,
        delight_threshold: f32,
        duration_seconds: u32,
        status: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RecordTrainingRunArgs {
                training_id,
                base_model,
                adapter_path,
                method,
                trajectory_count,
                group_count,
                epochs,
                final_loss,
                delight_threshold,
                duration_seconds,
                status,
            },
            callback,
        )
    }
}
