// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateCheckpointArgs {
    pub checkpoint_id: String,
    pub session_id: String,
    pub sandbox_id: String,
    pub snapshot_id: String,
    pub label: String,
    pub turn_number: u32,
    pub timestamp_ms: i64,
}

impl From<CreateCheckpointArgs> for super::Reducer {
    fn from(args: CreateCheckpointArgs) -> Self {
        Self::CreateCheckpoint {
            checkpoint_id: args.checkpoint_id,
            session_id: args.session_id,
            sandbox_id: args.sandbox_id,
            snapshot_id: args.snapshot_id,
            label: args.label,
            turn_number: args.turn_number,
            timestamp_ms: args.timestamp_ms,
        }
    }
}

impl __sdk::InModule for CreateCheckpointArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_checkpoint`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_checkpoint {
    /// Request that the remote module invoke the reducer `create_checkpoint` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_checkpoint:create_checkpoint_then`] to run a callback after the reducer completes.
    fn create_checkpoint(
        &self,
        checkpoint_id: String,
        session_id: String,
        sandbox_id: String,
        snapshot_id: String,
        label: String,
        turn_number: u32,
        timestamp_ms: i64,
    ) -> __sdk::Result<()> {
        self.create_checkpoint_then(
            checkpoint_id,
            session_id,
            sandbox_id,
            snapshot_id,
            label,
            turn_number,
            timestamp_ms,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `create_checkpoint` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_checkpoint_then(
        &self,
        checkpoint_id: String,
        session_id: String,
        sandbox_id: String,
        snapshot_id: String,
        label: String,
        turn_number: u32,
        timestamp_ms: i64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_checkpoint for super::RemoteReducers {
    fn create_checkpoint_then(
        &self,
        checkpoint_id: String,
        session_id: String,
        sandbox_id: String,
        snapshot_id: String,
        label: String,
        turn_number: u32,
        timestamp_ms: i64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateCheckpointArgs {
                checkpoint_id,
                session_id,
                sandbox_id,
                snapshot_id,
                label,
                turn_number,
                timestamp_ms,
            },
            callback,
        )
    }
}
