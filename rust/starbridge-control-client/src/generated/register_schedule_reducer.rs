// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RegisterScheduleArgs {
    pub schedule_id: String,
    pub agent_id: String,
    pub control_plane: String,
    pub goal: String,
    pub interval_minutes: u32,
    pub priority: String,
    pub requested_by: String,
    pub enabled: bool,
}

impl From<RegisterScheduleArgs> for super::Reducer {
    fn from(args: RegisterScheduleArgs) -> Self {
        Self::RegisterSchedule {
            schedule_id: args.schedule_id,
            agent_id: args.agent_id,
            control_plane: args.control_plane,
            goal: args.goal,
            interval_minutes: args.interval_minutes,
            priority: args.priority,
            requested_by: args.requested_by,
            enabled: args.enabled,
        }
    }
}

impl __sdk::InModule for RegisterScheduleArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `register_schedule`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait register_schedule {
    /// Request that the remote module invoke the reducer `register_schedule` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`register_schedule:register_schedule_then`] to run a callback after the reducer completes.
    fn register_schedule(
        &self,
        schedule_id: String,
        agent_id: String,
        control_plane: String,
        goal: String,
        interval_minutes: u32,
        priority: String,
        requested_by: String,
        enabled: bool,
    ) -> __sdk::Result<()> {
        self.register_schedule_then(
            schedule_id,
            agent_id,
            control_plane,
            goal,
            interval_minutes,
            priority,
            requested_by,
            enabled,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `register_schedule` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn register_schedule_then(
        &self,
        schedule_id: String,
        agent_id: String,
        control_plane: String,
        goal: String,
        interval_minutes: u32,
        priority: String,
        requested_by: String,
        enabled: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl register_schedule for super::RemoteReducers {
    fn register_schedule_then(
        &self,
        schedule_id: String,
        agent_id: String,
        control_plane: String,
        goal: String,
        interval_minutes: u32,
        priority: String,
        requested_by: String,
        enabled: bool,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RegisterScheduleArgs {
                schedule_id,
                agent_id,
                control_plane,
                goal,
                interval_minutes,
                priority,
                requested_by,
                enabled,
            },
            callback,
        )
    }
}
